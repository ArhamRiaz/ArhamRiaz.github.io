// Tailwind v4: font families are registered in app.css via @theme.
// This config only needs darkMode + content paths.
export default {
	darkMode: 'class',
	content: ['./src/**/*.{html,js,svelte,ts}'],
	plugins: []
};
